# Copie ce fichier en `.env` puis remplis les valeurs.
# IMPORTANT: ne commit jamais `.env` (il est ignoré via `.gitignore`).

# --- Broker / Marchés (paper trading) ---
ALPACA_API_KEY=
ALPACA_API_SECRET=
ALPACA_PAPER=true

# --- LLM ---
XAI_API_KEY=

# (optionnel) modèle pour `reflex_trader_agent.py`
REFLEX_TRADER_MODEL=grok-4-1-fast
